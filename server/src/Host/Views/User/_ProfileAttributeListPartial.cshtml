@model IEnumerable<UserClaimViewModel>

<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">
            Данные
        </h3>
    </div>
    <div class="panel-body">
        @if (Model.Count() > 0)
        {
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Название атрибута</th>
                        <th>Значение</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (UserClaimViewModel claim in Model)
                    {
                        <tr>
                            <td width="30%">@claim.ProfileAttributeName</td>
                            <td>
                               @Html.Partial("_ProfileAttributeDetailsPartial", claim)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="info">Список атрибутов профилей пуст</div>
        }
    </div>
</div>